import CreateDoctor from "./CreateDoctor";

// Both Create and Edit are unified in a single page/component so that
// all 3 sections (Identity & Credentials, Clinical Details, Schedule)
// and all fields are 100% identical and available for both create and update.
export default CreateDoctor;
